# Nextflow configuration
# Path to the Nextflow jar file (relative to project root)
# Use the -one.jar file which includes all dependencies
NEXTFLOW_JAR_PATH=nextflow/build/releases/nextflow-25.10.0-one.jar

# OpenRouter API Configuration (for pynf-agent)
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Specify a different model
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Optional: GitHub token for higher rate limits when fetching nf-core modules
# GITHUB_TOKEN=your_github_token_here
